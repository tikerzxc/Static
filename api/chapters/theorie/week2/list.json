---
---

[
{% for category in site.categories['theory-week2'] %}


  {
    "shortDesc"    : {{ category.title | jsonify }},
    "fullDesc"  :  {{ category.content | jsonify }},
    "type"  : {{ category.type | jsonify }},
    "src"  : {{ category.src | jsonify }},
    "linkItems" : [
      {% for subcategory in site.categories['theory-week2-links'] %}
        {
          "title":{{subcategory.title | jsonify}},
          "text":{{subcategory.content | jsonify}}
        } {% unless forloop.last %},{% endunless %}
      {% endfor %}
    ]

  } {% unless forloop.last %},{% endunless %}

{% endfor %}
]